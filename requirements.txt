# Core scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7

# Optional: only needed if you re-run the experiment
openai>=1.0
anthropic>=0.40
requests>=2.31

# Optional: only needed if you read .xlsx versions of the data
openpyxl>=3.1
